# Leave unset to build/demo against mock data (src/mock/hotspots.mock.json
# and simulated responses in src/api.js).
#
# Once the backend is deployed on Render, set this and every fetch call
# switches to the real pipeline automatically — no code changes needed.
# VITE_API_BASE_URL=https://<your-render-app>.onrender.com
